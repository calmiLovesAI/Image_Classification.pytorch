Train:
  epochs: 50
  batch_size: 8
  # 输入图片大小：(C, H, W)
  input_size:
    - 3
    - 224
    - 224
  learning_rate: 0.0001
  # 模型保存频率
  save_frequency: 5
  # 保存路径
  save_path: "saves"
Test:
  load_pth: ""
  test_pictures:
    - ""